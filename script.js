function calculateLoan() { const amount = parseFloat(document.getElementById("amount").value); const interest = parseFloat(document.getElementById("interest").value); const years = parseFloat(document.getElementById("years").value); if (isNaN(amount) || isNaN(interest) || isNaN(years)) { alert("يرجى إدخال جميع البيانات"); return; } const calculatedInterest = (interest / 100) / 12; const calculatedPayments = years * 12; const x = Math.pow(1 + calculatedInterest, calculatedPayments); const monthly = (amount * x * calculatedInterest) / (x - 1); if (isFinite(monthly)) { const total = monthly * calculatedPayments; const totalInterest = total - amount; document.getElementById("monthly-payment").innerText = monthly.toFixed(2); document.getElementById("total-payment").innerText = total.toFixed(2); document.getElementById("total-interest").innerText = totalInterest.toFixed(2); } }
